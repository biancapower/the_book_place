<p id="notice"><%= notice %></p>

<div class="container">
  <% @books.each do |book| %>
    <hr>
    <h3>
      <%= book.title %>
    </h3>
    <h5>
      <i><%= book.author %></i>
    </h5>
    <p>
      <strong>Your price:</strong> <%= book.price %>
    </p>
    <p>
      <strong>RRP:</strong> <%= book.recommended_retail_price %>
    </p>
    <p>
      <strong>Categories:</strong> <%= book.category %>
    </p>
    <p>
      <%= link_to 'View details', book, target: '_blank' %>
    </p>
    <hr>
  <% end %>
</div>


<!--
<h1>Books</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Title</th>
      <th>Author(s)</th>
      <th>Description</th>
      <th>Price</th>
      <th>Shipping cost</th>
      <th>Categories</th>

      <th>Recommended retail price</th>
      <th>Condition</th>
      <th>Format</th>
      <th>Publisher</th>
      <th>Pages</th>
      <th>Isbn10</th>
      <th>Isbn13</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>

        <td><%= book.user.id %></td>
        <td><%= book.title %></td>
        <td><%= book.author %></td>
        <td><%= book.description %></td>
        <td><%= book.price %></td>
        <td><%= book.shipping_cost %></td>
        <td><%= book.category %></td>

        <td><%= book.recommended_retail_price %></td>
        <td><%= book.condition %></td>
        <td><%= book.format %></td>
        <td><%= book.publisher %></td>
        <td><%= book.pages %></td>
        <td><%= book.isbn10 %></td>
        <td><%= book.isbn13 %></td>
        <td><%= link_to 'Show', book %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Book', new_book_path %>
-->
